<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.snowdog.dzialajlokalnie" >

    <!-- Creating Permission to receive Google Maps -->
    <permission
        android:name="pl.snowdog.dzialajlokalnie.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <!-- Permission to receive Google Maps -->
    <uses-permission android:name="pl.snowdog.dzialajlokalnie.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Maps API needs OpenGL ES 2.0. -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".DlApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="AA_DB_NAME"
            android:value="dzialajlokalnie.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="9" />
        <meta-data
            android:name="AA_MODELS"
            android:value="pl.snowdog.dzialajlokalnie.model.Category, pl.snowdog.dzialajlokalnie.model.Comment, pl.snowdog.dzialajlokalnie.model.District, pl.snowdog.dzialajlokalnie.model.Event, pl.snowdog.dzialajlokalnie.model.Issue, pl.snowdog.dzialajlokalnie.model.Session" />

        <activity
            android:name=".MainActivity_"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDEAHUJBu41zIFpk-kdVXMhCOw-DEAVzAQ" />

        <activity
            android:name=".IssueActivity_"
            android:label="@string/title_activity_issue"
            android:parentActivityName=".MainActivity_" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.snowdog.dzialajlokalnie.MainActivity_" />
        </activity>
    </application>

</manifest>
